<p>Ask a Question:</p>

<%= form_for @question do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :body, 'Question' %>
  <%= f.text_field :body %>
  <%= f.submit "Ask dat question", class: "submit" %>

<% end %>